---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu, tag: "16.04"}

inputs:
- name: pyfly-repo

run:
  # path: echo
  # args: ["hello world from Littleton, NH"]
  # path: pyfly/hello.sh 
  path: bash
  args:
  - -c
  - |
    ./pyfly-repo/configure_container.sh
