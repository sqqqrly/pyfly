---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu, tag: "16.04"}

inputs:
- name: pyfly-repo

run:
  # path: echo
  # args: ["hello world from Littleton, NH"]
  # path: pyfly/hello.sh 
  path: bash
  args:
  - -c
  - |
    whoami
    env
    find .
    mkdir -p some-output
    touch some-output/my-built-artifact
    find . -name my-build-artifact
    ./pyfly-repo/hello.sh
